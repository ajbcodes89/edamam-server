const { DataTypes } = require('sequelize');
const database = require('../db');

module.exports = database.define('recipes', {
    
    /*
    do this when we figure out what we want it to have
    : {
      type: DataTypes.STRING,
      allowNull: false 
    },
    : {
        type: DataTypes.INTEGER,
        
    },
})  */
